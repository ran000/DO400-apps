pipeline{
    agent any
    stages{
        stage("Deploy"){
            steps{
                sh '''
                    oc project developer-deploy-strategies
                    oc start-build greeting-service --follow --wait
                '''
            }
        }
    }
}
